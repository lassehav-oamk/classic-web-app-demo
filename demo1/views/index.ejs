<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diary Entries</title>
    <link rel="stylesheet" href="styles.css"> <!-- Optional: Link to an external CSS file for styling -->
</head>
<body>
    <div class="container">
        <h1>Demo 1 - Diary app with server only</h1>

        <% if (entries.length > 0) { %>
            <% entries.forEach(function(entry) { %>
                <div class="diary-entry">
                    <h2><%= entry.date %></h2>
                    <p><%= entry.content %></p>
                </div>
            <% }) %>
        <% } else { %>
            <p>No entries yet. Start writing your diary!</p>
        <% } %>

        <div class="form-container">
            <form action="/diary" method="POST">
                <input type="date" name="date" required>
                <textarea name="content" rows="5" placeholder="Write your diary entry here..." required></textarea>
                <button type="submit">Add Entry</button>
            </form>
        </div>
    </div>
</body>
</html>
